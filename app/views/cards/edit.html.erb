<section id="container">
  <div class="form-horizontal">
    <fieldset>
      <legend>редактировать карточку</legend>
      <div class="form-group">
        <%= render 'form', card: @card %>
      </div>
  </fieldset>
  </div>
</section>